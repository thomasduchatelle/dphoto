'use client';

import App from '../App';

// Root catch-all page that renders the existing App component
// This maintains the current React Router-based navigation
export default function RootPage() {
  return <App />;
}
